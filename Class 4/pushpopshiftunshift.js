Push adds a new element at the end of the array

const mobiles = ['iphone', 'samsung', 'oneplus', 'nokia', 'redmi'];

// add at the end
mobiles.push('17 Pro Max');

console.log(mobiles);

Pop method removes the element from the end of the array

const apple = ['iphone', 'samsung', 'htcplus', 'nokia', 'redmi'];

// remove from the end
apple.pop();
apple.pop();                
console.log(apple);

Shift removes the first element from the array

let names = ['parin', 'ventu', 'sathish', 'kumar', 'developer'];
// remove from the start
names.shift();
console.log(names);

// Unshift adds a new element at the start of the array

let names1 = ['parin', 'ventu', 'sathish', 'kumar', 'developer'];
// add at the start
names1.unshift('hello mobile');
console.log(names1);




